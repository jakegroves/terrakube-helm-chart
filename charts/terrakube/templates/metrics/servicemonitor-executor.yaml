{{- if and .Values.executor.enabled .Values.executor.metrics.serviceMonitor.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: terrakube-executor
  labels:
    {{- include "terrakube.labels" . | nindent 4 }}
    {{- with .Values.executor.metrics.serviceMonitor.additionalLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "terrakube.metricsSelectorLabels" (dict "svc" "executor" "root" $) | nindent 6 }}
  endpoints:
    - port: {{ .Values.executor.metrics.port | default "http" }}
      path: {{ .Values.executor.metrics.path | default "/actuator/prometheus" }}
      interval: {{ .Values.executor.metrics.interval | default "30s" }}
      # the app emits its own `service` tag; let it win over the Operator's target label
      honorLabels: true
      {{- with .Values.executor.metrics.serviceMonitor.relabelings }}
      relabelings:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end -}}
