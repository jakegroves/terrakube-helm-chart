{{- if and .Values.registry.enabled .Values.registry.metrics.serviceMonitor.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: terrakube-registry
  labels:
    {{- include "terrakube.labels" . | nindent 4 }}
    {{- with .Values.registry.metrics.serviceMonitor.additionalLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "terrakube.metricsSelectorLabels" (dict "svc" "registry" "root" $) | nindent 6 }}
  endpoints:
    - port: {{ .Values.registry.metrics.port | default "http" }}
      path: {{ .Values.registry.metrics.path | default "/actuator/prometheus" }}
      interval: {{ .Values.registry.metrics.interval | default "30s" }}
      # the app emits its own `service` tag; let it win over the Operator's target label
      honorLabels: true
      {{- with .Values.registry.metrics.serviceMonitor.relabelings }}
      relabelings:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end -}}
